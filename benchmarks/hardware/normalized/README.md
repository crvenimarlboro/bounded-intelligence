# Normalized hardware results

Derived files in this directory are ignored and regenerated from immutable files in `../raw` using
the documented `bilab bench ingest` command. The raw sources remain authoritative.
