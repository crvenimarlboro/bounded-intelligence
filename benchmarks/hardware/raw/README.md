# Immutable source reports

These are byte-for-byte copies of the small text reports originally observed under
`/mnt/e/AI/benchmarks` on 2026-07-22. Do not edit them; replace/add only with provenance and hashes.
Native llama-bench JSONL/CSV files are normalization inputs. Human/derived summary CSV/TXT files are
retained as provenance but the ingester intentionally warns and skips non-native schemas. Verify
copies from this directory with `sha256sum -c SHA256SUMS`.
